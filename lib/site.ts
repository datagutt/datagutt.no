// The site's public address and link-preview image, for metadata, the sitemap and robots.

export const SITE_URL = "https://datagutt.no";

/**
 * Drawn by `pnpm assets` (scripts/assets/og.mjs) from the licensed art, so it is built,
 * not committed. A page's `openGraph` replaces the layout's whole, so pages that set
 * their own repeat it.
 */
export const OG_IMAGE = { url: "/game/og.png", width: 1200, height: 630, alt: "A pixel-art town on a fjord under the word DATAGUTT." };
